# AI 开发协作协议

本文件适用于所有参与 Novel Studio 的 AI 和自动化工具。

## 开始任务前

必须依次阅读：

1. `PROJECT.md`
2. `docs/product/README.md`
3. `docs/product/PRODUCT_SPEC.md`
4. `docs/product/REQUIREMENTS_TRACEABILITY.md`
5. `STATUS.md`
6. `HANDOFF.md`
7. `TASKS.md`
8. `docs/architecture/TARGET_ARCHITECTURE.md`
9. 当前任务对应的领域规格、架构文档与 ADR

还必须阅读 `docs/README.md` 和当前任务对应的验收记录。开始编码前，把任务规格中的每条验收 ID 映射到现有测试、拟新增测试或明确的人工验证；无法映射的验收不得被默认忽略。

一次只处理一个 `NS-###` 任务。不得顺手扩展范围、重写无关模块或把路线图中的占位界面描述成已完成功能。

聊天上下文不是项目需求的长期来源。若发现重要产品决定只存在于聊天中，先补入权威规格再实现。若需求不明确，不得根据当前代码猜测；在 `STATUS.md` 记录产品疑问并请求用户决定。

涉及 UI、布局或文案的任务还必须阅读 `docs/product/USER_EXPERIENCE_SPEC.md`。实现时从作者视角判断信息层级：写作、规划和审阅主界面不得展示调用 ID、基准版本、来源哈希、内部任务名等工程审计字段；这些信息只能进入记录、详情页或调试视图。新增界面必须复用统一视觉语言和组件风格，不得每个页面临时发明一套按钮、卡片、字体、留白或色彩。主界面不得写成操作手册；“如何使用”只能以短标签、输入框 placeholder、图标 tooltip、短空状态和必要错误提示轻量出现，不能用教程卡、大段说明或常驻帮助文案挤占正文和工作区。

NS-409 前端重做必须先做可维护架构，不得先做整页。固定边界为 `src/ui/` 放设计令牌和基础组件，`src/features/<domain>/` 按业务域组合页面，`src/api/` 独立接口层，`src/app/` 放应用壳和全局边界。全局样式只允许 `reset.css`、`tokens.css` 和少量 layout 基础，不得重新出现几千行 `styles.css` 控全局。全量实现前必须先交付英文 UI 的写作、规划、设置三个核心页面设计，并先完成“作品选择 → 写作页 → 保存状态 → 设置入口”垂直切片；每个控件必须来自组件库。

## 实施约束

- NS-410 起，作品权威数据改为项目目录中的结构化 JSON 文件；Markdown/Word 只作为导入、导出、镜像和迁移边界格式。索引数据库、缓存、localStorage 和编辑器运行时 JSON 不得成为唯一副本。
- AI、批量替换、Word 导入等写入必须经过 Proposal 或显式用户操作。
- 所有文件写入使用同目录临时文件、校验和原子替换。
- 不记录 API 密钥、完整私密正文或未脱敏请求头。
- 不增加遥测、账户、远程监听或自动云端回退。
- 新依赖必须说明用途和许可证；优先宽松许可证。
- 变更数据格式或数据库结构时新增 ADR、迁移和回滚测试。
- 除非用户明确说明，任何修复不得减少、隐藏、绕过或降级原本已有的功能；若修复必须临时停用或替代某项能力，必须先记录影响范围并请求用户确认。
- 不得以“先简化”为由移除 Proposal、证据、权限、故事时间或角色知识等产品核心约束。
- 产品范围变化必须同步更新产品规格、需求追踪、ADR、任务与验收，不能只改代码。
- UI 变更必须同步检查中文文案是否自然、是否存在中英文混搭或翻译腔；不能把数据字段名直接搬进用户界面。

## 完成任务前

1. 运行任务要求的测试，记录实际命令和结果。
2. 更新 `STATUS.md`、`TASKS.md`、`HANDOFF.md` 和必要的 `CHANGELOG.md`。
3. 确保工作区只包含任务相关变更。
4. 提交格式：`NS-### type(scope): summary`。

完成声明还必须满足：

- 对损坏输入、遗漏成员、重复 ID、过期版本和中途失败至少进行与任务风险相称的对抗测试。
- 由实现者重新逐条检查“规格不变量 → 测试 → 实际结果”，不能用测试总数代替覆盖证明。
- 工作树状态、分支、提交、脏文件和测试数量必须按实际命令记录；未提交或仍有任务相关脏文件时保持“进行中”。
- 不能由 no-op 路径证明迁移、恢复或回滚成功；fixture 必须真实进入待迁移或故障状态。

`main` 必须始终可构建且测试通过。顺序接力默认继续当前里程碑分支；并行工作必须先分配模块所有权。
## 文档清理规则

- 整理项目记录时，默认更新既有入口文档：`STATUS.md`、`HANDOFF.md`、`TASKS.md`、`CHANGELOG.md`、当前 `docs/tasks/NS-*.md` 和当前 `docs/testing/NS-*_ACCEPTANCE.md`。不得为普通状态记录新建额外文档。
- `docs/design/**/backups/` 是用户确认过的历史基准备份，默认必须保留。除非用户明确点名允许删除某个备份目录或文件，不得把备份当作过时文档清理。
- 可删除对象仅限已经被当前入口文档取代的临时验证记录、失败中间截图、重复草稿和明确标记废弃的非备份资产；删除前必须先确认它不在当前任务、验收记录或 README 的有效引用链上。
- UI 实现记录必须区分“命令验证通过”和“用户视觉验收通过”。用户视觉未通过时，不得把阶段写成完成，也不得把当前截图或实现当作新基准。
- 新增 UI 文案必须集中在 `uiText`、feature view model 或后续 i18n 资源中；组件内不得继续硬编码混合语言文案。
