# NS-514 Reference UI Rebuild

Status: in_progress
Created: 2026-07-15
Owner: Codex
Acceptance: `docs/testing/NS-514_ACCEPTANCE.md`

## Objective

Rebuild the Novel Studio frontend from the tracked binding reference HTML in a
strict sequence: recover the pre-NS-514 runtime baseline, extract a complete
reference contract, reproduce the reference without legacy-UI additions,
obtain explicit author visual approval, audit reference controls against real
capabilities, and only then wire explicitly approved capabilities in place.

The detailed phase contract is
`docs/tasks/NS-514_REFERENCE_UI_REBUILD_PLAN.md`. The binding visual source is
`docs/design/ui-redesign/novel-studio-full-ui-redesign-reference.html`.

This task record is only an execution and evidence index for that existing
plan. It does not redefine, extend, or reinterpret the plan. P0 through P4 are
an exact reference reproduction, not a merge with the old UI. The P5/P6 audit
and in-place wiring described by the plan cannot introduce old UI structure;
they begin only after the P4 replica is explicitly accepted.

Canonical author-facing hierarchy: `Series → Volume → Chapter → Act → Scene`.

NS-514 does not imply that NS-511, NS-512, or NS-513 is complete. Their Prompt,
Tool Plan/Grant, Council, conflict/failure, and earlier visual-acceptance gates
remain open unless separately proved by their own acceptance records.

## Required Reading

- `docs/product/PRODUCT_SPEC.md` sections 3 through 10 and 16 - author
  sovereignty, honest capability boundaries, unified visual language,
  hierarchy, workspaces, Write, Codex, Workshop, Prompt, and completion.
- `docs/product/USER_EXPERIENCE_SPEC.md` sections 1 through 10 and 12 - visual
  language, maintainability, shell, core workspaces, Review, and degraded
  states.
- `docs/product/REQUIREMENTS_TRACEABILITY.md` M5 - existing milestone behavior
  and unfinished gates.
- `docs/product/AI_EDITORIAL_SYSTEM.md` - Workshop, Context, Proposal, Review,
  Prompt, Provider, and author-confirmation boundaries.
- `docs/architecture/ARCHITECTURE.md`,
  `docs/architecture/TARGET_ARCHITECTURE.md`, and
  `docs/architecture/SECURITY.md` - current frontend/API ownership, JSON
  authority, command safety, credentials, and local-only runtime.
- `docs/tasks/M5.md` and `docs/testing/M5_ACCEPTANCE.md` - unfinished M5
  behavior and visual gates that the rebuild must not overstate.
- `docs/design/ui-redesign/FIGMA_TO_IMPLEMENTATION_WORKFLOW.md`,
  `docs/design/ui-redesign/M5_WORKSHOP_REVIEW_FIGMA_PLAN.md`, and
  `docs/design/ui-redesign/M5_FIGMA_UI_REVIEW.md` - component, route, state,
  evidence, and user-acceptance constraints that remain applicable where they
  do not conflict with the newer author-directed reference source.
- `docs/tasks/NS-514_REFERENCE_UI_REBUILD_PLAN.md` - binding phase order,
  reference inventory, recovery boundary, and completion definition.
- `docs/design/ui-redesign/novel-studio-full-ui-redesign-reference.html` -
  binding structure, styles, visible states, interactions, and responsive
  behavior for the reproduction pass.

## In Scope

- P0: prove that tracked Web runtime files match the pre-NS-514 `HEAD`, remove
  only rejected untracked NS-514 runtime files if any exist, preserve all
  unrelated worktree changes, and prove the baseline Web build.
- P1: create docs/tasks/NS-514_REFERENCE_MANIFEST.md as the human-readable
  and machine-checkable inventory of tokens, regions, controls, states,
  interactions, media queries, and fixture-only content.
- P2: port the reference application frame, top appbar, workspace switcher,
  project/library menu, and New Series dialog without legacy shell additions.
- P3: port Overview, Settings, Plan, Write, Codex, Workshop, and the reference
  overlays in the order fixed by the rebuild plan.
- P4: assemble a reference-only replica, verify its declared structure and
  interactions, and stop for explicit author visual acceptance.
- P5: create
  docs/tasks/NS-514_REFERENCE_CONTROL_AND_CAPABILITY_AUDIT.md, covering each
  reference control and each real existing capability exactly once.
- P6: replace fixtures and connect controls only where the author-approved
  disposition preserves the reference node, structure, placement, and visual
  state while retaining existing safety and authority boundaries.
- P6 Codex decision package: record the approved Entry, Detail, Relation,
  Current Scene, archived-category, filter-removal, and navigation dispositions
  before implementation. Relation implementation includes the explicitly
  approved schema v2 migration and guarded permanent-delete command governed by
  ADR-0015.
- P7: run focused and regression checks, record exact remaining gaps and
  deviations, and obtain final explicit author visual acceptance.

## Out Of Scope

- Treating the reproduction fixtures as production project data or successful
  backend operations.
- Adding legacy controls, substitute menus, routes, cards, banners, or panels
  that are absent from the reference during P1 through P4.
- Changing backend contracts, authority schemas, migrations, Provider
  behavior, or command safety merely to make the static reference work. The
  author-approved Relation v2 change in ADR-0015 is a narrow exception for the
  later Codex P6 implementation; it is not authorized merely by rendering the
  reference.
- Claiming that disabled Review or Research controls in the reproduction pass
  remove those product requirements or supersede their existing real APIs.
- Completing NS-511 versioned Workshop Prompt authority, NS-512 Tool
  Plan/Grant behavior, or NS-513 Council/final M5 gates by implication.
- Agent-owned visual acceptance or screenshot-loop approval.

## Logical Closure Boundary

| Boundary | Handling |
| --- | --- |
| Authoritative data | Unchanged through P4; fixture state is isolated and non-authoritative. P6 normally reads/writes through approved APIs and commands. The approved Codex slice must advance Relation authority from v1 to v2 exactly as ADR-0015 specifies rather than fabricating a hidden `type`. |
| Read paths | P1 inventories the reference; P5 maps every real frontend/API read path before P6 integration. |
| Write paths | No product authority writes through P4. P6 preserves Proposal, Grant/confirmation, revision, snapshot, and repository-command boundaries. Codex Entry, Detail, Detail Type, Relation, and navigation actions use real commands and real success/error results. |
| Derived state | Existing caches/indexes remain rebuildable and are not replaced by UI fixture state. Current Scene is a read-only projection at the Write-selected Scene; Relation v2 is the only approved schema change in this slice. |
| User entry points | Reference appbar, project menu/dialog, workspace switching, domain controls, and overlays; absent legacy entry points remain an explicit P5 decision. |
| Lifecycle | Entry context menus expose Archive/Delete or Restore/Delete according to real state. Detail Type and Relation use confirmed context-menu deletion with server-owned reference blockers. Relation has no new author-facing archive action. |
| Migration/rollback | Source rollback remains the pre-NS-514 `HEAD` Web baseline recorded at P0. Relation v1-to-v2 migration additionally requires an exact pre-migration rollback artifact, validation, atomic replacement, damaged-input coverage, and exact v1 restoration under ADR-0015. |
| Detail Type migration/rollback | Detail Type schema v2 adds the author-written `description` field under ADR-0016. Compatibility reads project v1 as an empty description without writing. Explicit migration validates every file before an atomic replacement set, records exact v1 authority and checksums, and rollback restores exact v1 files only while the migrated v2 files remain unchanged. |
| Fixtures | Reference fixture text/counts are allowed only in isolated reproduction fixtures/tests and must be removed or explicitly classified before production wiring. |
| Acceptance | `docs/testing/NS-514_ACCEPTANCE.md`; P4 and P7 each retain an explicit author visual gate. |

## Decisions And Open Questions

- On 2026-07-15 the author directed work to start on NS-514. The tracked
  reference HTML and the existing rebuild plan are therefore the binding
  structural source for P0 through P4.
- The original NS-514 rebuild plan owns the implementation direction. This
  execution record does not independently change product or UX specifications.
- Reference colors, typography, dimensions, spacing, and breakpoints override
  generic UI-skill recommendations. The skill guidance is retained only for
  semantic HTML, keyboard/focus behavior, honest disabled states, component
  ownership, and accessibility checks that do not alter the reference.
- Review and Research remain disabled exactly as shown during the reproduction
  pass. This is an approved staging state, not approval to remove existing
  product capabilities from the final connected UI.
- P4 acceptance authorizes the capability audit, not arbitrary integration.
  Every capability absent from the reference and every mapping that would
  change visible structure remains an explicit author decision at P5.
- "Wire approved controls in place" has the plan's narrow meaning: replace a
  fixture/local handler inside the same accepted reference control. It never
  authorizes combining the old and reference UIs or restoring an old control
  into the reference layout.
- No visible deviation from the reference is currently approved.
- The P3/P4 reproduction components consume the tracked binding HTML's exact
  DOM fragments, stylesheet, and local-only reference runtime at build time.
  This is a non-visible fidelity mechanism for the reproduction gate, not a
  production-data integration decision; P5/P6 must still classify and replace
  every fixture/local handler only after author approval.
- On 2026-07-15, after the P5 audit purpose and its separation from P6 wiring
  were restated, the author directed work to start. This records explicit P4
  visual acceptance and authorizes P5 audit creation only; it does not approve
  any control mapping, visible deviation, or backend integration.
- P5 is complete in
  `docs/tasks/NS-514_REFERENCE_CONTROL_AND_CAPABILITY_AUDIT.md`: all 368
  reference controls, 80 reachable old-UI API methods, 21 separately reviewed
  frontend-local capabilities, and seven excluded old fake/placeholder controls
  are accounted for. All 469 decision rows remain `pending_author`; P6 remains
  unauthorized until the author explicitly approves selected in-place mappings.
- On 2026-07-15 the author prohibited delete-and-readd replacement of an
  existing file. The durable rule is recorded in root `AGENTS.md`: existing
  paths must be updated in place, and oversized generated artifacts must be
  redesigned rather than temporarily removed.
- The author also prohibited using `apply_patch` to delete large files. For an
  explicitly authorized deletion, the path and size must be verified first;
  files at least 128 KiB use native `Remove-Item -LiteralPath` and are never
  deleted as part of a replace/rebuild sequence.
- Detailed P5 page comparisons requested by the author are recorded in:
  - `docs/tasks/NS-514_SETTINGS_UI_COMPARISON.md`
  - `docs/tasks/NS-514_WRITE_UI_COMPARISON.md`
  - `docs/tasks/NS-514_CODEX_UI_COMPARISON.md`
  - `docs/tasks/NS-514_WORKSHOP_UI_COMPARISON.md`
  These documents compare new-reference and pre-NS-514 functions in both
  directions and do not authorize P6 wiring.
- On 2026-07-15 the author approved P6 work for the Write surface only. The
  approved dispositions are: connect the complete existing NovelEditor and
  lightweight current-position Canon Description preview; redesign the full
  Story Change lifecycle in the accepted Write visual language with its add
  control at `right sidebar -> Scene page -> Story changes heading`; retain
  resizing and require delete confirmation; connect compact animated autosave
  with three automatic retries and no manual retry; add title context menus for
  rename/delete on Volume through Scene but not Series; keep every unsupported
  reference control visually present and disabled; and fix the disabled writing
  mode at Draft. No Settings, Codex, Workshop, or other P6 audit disposition is
  authorized by this decision.
- On 2026-07-15 the author explicitly rejected the first connected Story Change
  visual treatment and directed a complete UI redo. The rejection changes the
  presentation only: the approved Scene-page Add placement and the retained
  create/edit/collapse/resize/reorder/keyboard-move/focus/confirmed-delete
  lifecycle remain binding. The replacement is a Write-only presentation and
  still requires explicit author visual acceptance under A16.
- On 2026-07-15 the author approved the following Codex P6 dispositions for
  specification and acceptance planning. This decision record does not start
  Codex implementation; implementation still requires a subsequent explicit
  author direction:
  - Active Entry context menus provide Archive and Delete; archived Entry
    context menus provide Restore and Delete. Menus are keyboard reachable and
    permanent deletion requires confirmation.
  - The three Story Lens controls are removed. Archived Entries replaces their
    area as a real Entry category, and the unsupported Watch filter is removed.
    `In Scene` and `Changed` are also removed; no semantic filter work remains
    for these three controls.
  - Category Rename/Delete uses a keyboard-reachable context menu. The old
    explicit Entry Reload control is removed and must not be restored.
  - Add Detail appends one row to Structural Details. Detail Type is a select,
    Value is a text editor, and the third column is Save until creation
    succeeds; the normal row then exposes its Send to AI switch. Detail Type
    deletion moves to a keyboard-reachable context menu and retains the
    existing in-use blocker. An already-saved Detail row also exposes confirmed
    Delete through its row context menu.
  - Add Relation opens a simple From, To, and required Simple Description
    page. The author explicitly rejected retaining or merging historical
    Relation `type`; ADR-0015 therefore defines a v1-to-v2 migration that drops
    `type` and a rollback artifact that restores exact v1 authority. Relation
    rows provide confirmed guarded Delete and no author-facing Archive.
  - `Effective Scene` is renamed `Current Scene`. Baseline remains editable;
    Current Scene follows the Scene currently selected in Write and exposes a
    read-only effective Canon and Details projection. Research remains
    Baseline, and the blue Canon hint box is removed from Current Scene.
  - Open in Write and Open Scene use their existing reference positions and are
    enabled only for real targets.
  - Mentions retains the existing Manuscript source and adds a Codex source for
    mentions from other Codex content. Canon Description mentions reuse the
    Write inline mention marker and preview the referenced Entry's Canon
    Description summary. This changes only the popover opened from a highlighted
    Entry inside the Codex Canon page: Baseline mode shows the referenced
    Entry's Baseline Canon summary, while Current Scene mode shows that Entry's
    effective Canon summary at the Scene currently open in Write. The separate
    Mentions page and its Manuscript/Codex source selection do not change with
    this switch.
- On 2026-07-16 the author explicitly directed formal Codex implementation to
  start and then directed it to continue. The approved A22-A28 slice is now
  implemented with real project-session data and API results. Automated
  contract, storage, server, Web, migration, rollback, blocker, and build
  evidence is recorded in the acceptance record. This does not authorize
  Settings or Workshop integration, and it does not pass the A16 author visual
  gate.
- On 2026-07-16 the author rejected the connected Detail Type Library because
  it replaced the approved three-column new-UI dialog with an unauthorized
  two-column inline-create design. The correction must restore the reference
  Category column, Detail Type list column, and selected Detail Type editor
  column. The author additionally requires a real persisted Detail Type
  description; ADR-0016 governs its schema version 2 migration and rollback.
- On 2026-07-16 the author additionally required Detail Type Rename to be a
  real action beside Delete in the keyboard-reachable Detail Type row context
  menu. Rename uses a focused name editor, revision protection, same-Category
  duplicate-name validation, and an atomic conversion of any legacy Entry
  Detail keys from the old display name to the stable Detail Type identifier.

## Acceptance IDs

| ID | Atomic requirement | Planned exact proof |
| --- | --- | --- |
| NS-514-A01 | P0 restores and proves the tracked pre-NS-514 Web baseline, contains no rejected untracked runtime file, preserves the tracked reference, and builds successfully | `git diff --name-status HEAD -- apps/web`; `git ls-files --others --exclude-standard -- apps/web`; tracked reference blob/hash check; `npm.cmd run build -w @novel-studio/web` |
| NS-514-A02 | Task/index/status and the P0 evidence agree without marking NS-511 through NS-513 complete or altering unrelated worktree items | `tests/docs/docs-check.test.mjs` — `keeps support work separate from the sequential M-to-NS mainline`; `npm.cmd run docs:check`; `git diff --check`; acceptance-record and worktree review |
| NS-514-A03 | P1 manifest indexes every reference token group, top-level region, workspace, overlay, and child-region order | planned `tests/docs/ns-514-reference-contract.test.mjs` — `indexes every binding reference surface and ordered region` |
| NS-514-A04 | P1 manifest indexes every default/selected/hidden/disabled/expanded state, interaction trigger, media query, and fixture-only datum without treating current APIs as reference requirements | planned `tests/docs/ns-514-reference-contract.test.mjs` — `maps reference states interactions breakpoints and fixtures without runtime leakage` |
| NS-514-A05 | P2 shell DOM, class contract, default active/disabled state, project menu, and New Series dialog match the manifest without an old sidebar or extra control | planned `apps/web/src/app/ReferenceAppShell.test.tsx` — `renders only the manifest shell regions in reference order` and `preserves the reference default workspace and disabled navigation` |
| NS-514-A06 | P2 reference-defined switching, dismissal, validation, focus, Escape, and compact behavior work through semantic controls | planned `apps/web/src/app/ReferenceAppShell.test.tsx` — `reproduces project menu and New Series dialog interactions` and `keeps reference shell interactions keyboard reachable` |
| NS-514-A07 | P3 ports Overview, Settings, Plan, Write, Codex, Workshop, and reference overlays with their ordered regions, visible labels, and declared states and no legacy additions | planned domain tests with exact `matches the NS-514 manifest for <workspace>` cases under the owning `apps/web/src/features/<domain>/` test files |
| NS-514-A08 | P3 ports only reference-defined workspace interactions and responsive state changes while keeping fixture operations isolated from production authority | planned `apps/web/src/app/ReferenceReplica.test.tsx` — `reproduces declared workspace interactions without calling product APIs`; planned `tests/e2e/ns-514-reference-structure.spec.ts` — `matches declared desktop and compact structural states` |
| NS-514-A09 | P4 assembled replica exposes only reference-defined visible structure and complete reference switching/overlay return paths | planned `apps/web/src/app/ReferenceReplica.test.tsx` — `assembles every reference surface without non-reference controls` and `switches workspaces and closes overlays through declared return paths` |
| NS-514-A10 | The author explicitly accepts or rejects the P4 reference-faithful replica before capability audit/integration starts | named manual procedure `P4 author reference-replica visual review`; agent diagnostics cannot pass it |
| NS-514-A11 | P5 lists every visible reference control exactly once with node, state, interaction, frontend/API mapping, disposition, and structural-fit decision | planned `tests/docs/ns-514-reference-audit.test.mjs` — `accounts for every manifest control exactly once` |
| NS-514-A12 | P5 lists every real pre-NS-514 UI capability exactly once with old entry, callback, API, absence impact, future options, and author decision | planned `tests/docs/ns-514-reference-audit.test.mjs` — `accounts for every inventoried runtime capability exactly once` plus named manual source review |
| NS-514-A13 | P6 uses real runtime data and real success/error results only for approved in-place mappings; fixtures and fake counts never reach production pages | planned focused domain tests named `uses real <domain> data in the approved reference fields` plus `apps/web/src/app/ReferenceReplica.test.tsx` — `does not expose fixture-only story data in production mode` |
| NS-514-A14 | P6 retains Proposal, permissions/Grant, revision, atomic-write, destructive-confirmation, context, and story-time/knowledge safety boundaries | existing affected Web/server/storage contract tests plus new exact regression names recorded before each approved mapping is implemented |
| NS-514-A15 | P7 required focused tests, Web typecheck/build, applicable regressions, docs/link checks, structure diagnostics, deviations, gaps, branch, commit, and dirty-file state are recorded from actual output | `npm.cmd run typecheck -w @novel-studio/web`; `npm.cmd run test -w @novel-studio/web`; `npm.cmd run build -w @novel-studio/web`; affected package tests; `npm.cmd run docs:check`; `git diff --check`; acceptance ledger |
| NS-514-A16 | The author explicitly accepts the final connected UI; diagnostic screenshots, DOM measurements, builds, and test counts remain non-acceptance evidence | named manual procedure `P7 final author visual acceptance` |
| NS-514-A17 | The approved Write P6 slice renders real hierarchy, Scene title/document, live counts, complete NovelEditor behavior, and a lightweight story-position-safe Canon Description preview without fixture manuscript claims | `apps/web/src/features/write/ReferenceWriteWorkspace.test.tsx` — `connects real Write data and the complete NovelEditor without fixture manuscript claims`; existing `apps/web/src/features/write/editor/NovelEditor.test.tsx` preview and editor regressions |
| NS-514-A18 | Autosave uses one compact animated state slot, performs one initial attempt plus exactly three automatic retries for ordinary failures, stops at Failed, and starts a new cycle only after the next edit; conflicts are never retried | `apps/web/src/app/useProjectSession.test.tsx` — `retries a failed autosave exactly three times and waits for the next edit before a new cycle` and `does not retry a revision conflict`; `ReferenceWriteWorkspace.test.tsx` — `connects real Write data and the complete NovelEditor without fixture manuscript claims` |
| NS-514-A19 | Story Change creation is in the Scene page Story changes heading, while create/edit/collapse/resize/reorder/keyboard move/focus/delete remain connected and deletion requires confirmation | `apps/web/src/features/write/ReferenceWriteWorkspace.test.tsx` — `manages Story changes from the Scene page without adding an editor insertion control` and `renders Story Change as a labeled Write surface while preserving its legacy actions`; existing `NovelEditor.test.tsx` and focused legacy regressions |
| NS-514-A20 | Volume, Chapter, Act, and Scene titles expose keyboard-reachable context menus for inline rename and confirmed delete with cascade impact, while Series exposes neither action | `apps/web/src/features/write/ReferenceWriteWorkspace.test.tsx` — `renames and confirms deletion from hierarchy title context menus without exposing Series lifecycle actions` |
| NS-514-A21 | Unsupported Write controls remain visually present but semantically disabled, Draft is the fixed default, and enabled controls never simulate fixture-only success | `apps/web/src/features/write/ReferenceWriteWorkspace.test.tsx` — `connects real Write data and the complete NovelEditor without fixture manuscript claims`; `apps/web/src/app/ReferenceReplica.test.tsx` — `omits fixture Write behavior when the connected Write workspace owns its interactions` |
| NS-514-A22 | Codex uses real categories and entries, removes all three Story Lenses plus In Scene/Changed/Watch, exposes a real Archived Entries category, omits Reload, and manages Category and active/archived Entry lifecycle through keyboard-reachable confirmed context menus | planned `apps/web/src/features/codex/ReferenceCodexWorkspace.test.tsx` — `loads real Codex entries and manages category and entry lifecycle from keyboard-reachable context menus` |
| NS-514-A23 | Add Detail appends the approved editable row, converts it to a normal AI-policy row only after a real save, and saved Detail plus Detail Type context-menu deletion preserve confirmation, revision, and in-use blocking | planned `apps/web/src/features/codex/ReferenceCodexWorkspace.test.tsx` — `adds and deletes structural Detail rows and deletes an unused Detail Type from context menus`; affected storage/server Entry and Detail Type regressions |
| NS-514-A24 | Relation v2 removes `type`, requires From/To/description for new writes, migrates v1 without preserving or merging `type`, restores exact v1 rollback data, and hard-deletes only unreferenced Relations after confirmation | planned `packages/contracts/test/codex.test.ts` — `accepts description-only Codex relation v2 documents without type`; `packages/storage/test/repository.test.ts` — `migrates v1 Codex relations by dropping type and restores exact rollback backups` and `hard deletes only unreferenced Codex relations`; `apps/server/test/app.test.ts` hard-delete route case; `apps/web/src/features/codex/ReferenceCodexWorkspace.test.tsx` — `creates and deletes a description-only relation without archive controls` |
| NS-514-A25 | Baseline is editable while Current Scene is a read-only Canon/Details projection at the Write-selected Scene, Research remains Baseline, and no future state or blue Canon hint leaks into the view | planned `apps/web/src/features/codex/ReferenceCodexWorkspace.test.tsx` — `switches Baseline and Current Scene using the Write-selected Scene without future leakage`; existing and affected storage/server effective-entry regressions |
| NS-514-A26 | Progression and Mention navigation enables Open in Write/Open Scene only for real targets and resolves the actual Scene or block without simulated success | planned `apps/web/src/features/codex/ReferenceCodexWorkspace.test.tsx` — `opens real Progression and Mention targets in Write` |
| NS-514-A27 | Mentions separates real Manuscript and Codex sources. A highlighted Entry inside the Codex Canon page uses the manuscript-style popover: Baseline mode shows the referenced Entry's Baseline Canon summary, and Current Scene mode shows its effective Canon summary at the Scene currently open in Write; the separate Mentions page does not change source or content because of this view switch | planned `apps/web/src/features/codex/ReferenceCodexWorkspace.test.tsx` — `keeps Mention sources stable while Canon popovers follow Baseline and Current Scene`; existing Write/NovelEditor mention-preview regressions |
| NS-514-A28 | Detail Type Library restores the approved three-column new-UI structure and real Category/Detail Type selection, persists version 2 Detail Type descriptions, keeps unsupported editor fields visibly disabled, and enters real Rename plus confirmed guarded Delete only from the row context menu. Rename rejects same-Category duplicate names and atomically converts legacy name-keyed Entry Detail and context-policy data to the stable Detail Type identifier without overwriting a conflicting value | `packages/contracts/test/codex.test.ts` — `accepts Detail Type v2 descriptions and projects v1 without inventing text`; `packages/storage/test/repository.test.ts` — `migrates v1 Codex Detail Types with empty descriptions and restores exact rollback backups` plus the planned Detail Type rename and legacy-key conversion case; `apps/server/test/app.test.ts` — `persists Detail Type descriptions through create update and list` extended with Rename; `apps/web/src/features/codex/ReferenceCodexWorkspace.test.tsx` — `reproduces the three-column Detail Type Library and persists descriptions` extended with the keyboard context-menu Rename flow |

## Completion Criteria

- P0 through P7 pass in order and every acceptance ID has actual evidence.
- The P4 and P7 author visual gates are explicitly recorded; neither may be
  inferred from diagnostics, screenshots, tests, or builds.
- Every reference control and pre-NS-514 capability has one audited disposition
  and all visible deviations have explicit author approval.
- Production pages contain real data or honest unavailable/disabled states,
  never reference fixture claims.
- Existing authority, safety, context, Proposal, permission, and lifecycle
  behavior is preserved for every connected control.
- Task-related changes are committed independently while unrelated user changes
  remain untouched.
