# M5 Workshop, Proposal, And Review

Status: M5.1-M5.5 command/function verified; M5.6 next; user visual acceptance pending
Created: 2026-06-30  
Started: 2026-07-01
Owner: Codex  
Acceptance record: `docs/testing/M5_ACCEPTANCE.md`  
Design companion: `docs/design/ui-redesign/M5_WORKSHOP_REVIEW_FIGMA_PLAN.md`  
Figma UI review: `docs/design/ui-redesign/M5_FIGMA_UI_REVIEW.md`  
Figma implementation workflow: `docs/design/ui-redesign/FIGMA_TO_IMPLEMENTATION_WORKFLOW.md`

## Decision

M5 must make Proposal and Review reliable before Workshop becomes a broad chat workspace.

The implementation order is:

```text
protect current Write/Codex behavior
  -> define Proposal v2 and Review state machine
  -> implement Proposal storage/API and Review decisions
  -> implement Review UI on real Proposal data
  -> implement Workshop sessions/context/single-role call
  -> let Workshop messages create Proposals with exact Review deep links
  -> add authorized internal tool plans and command adapters
  -> add council, batch, conflict/failure, responsive, and visual acceptance coverage
```

Workshop is important, but it is not the first authority-changing surface. The durable value of Workshop output is the Proposal/Review loop. A chat panel that can produce cards but cannot reliably create, stale-check, apply, reject, or audit Proposals is not an accepted M5 result.

## Baseline

M5 starts after `NS-410` post-Slice 11 audit repair is command-verified and Project Recovery is accepted for the current stage by user decision on 2026-06-30.

That acceptance is the baseline for continuing into M5. It does not mean M5 Workshop, Proposal, Review, Tool Plan, or Council behavior is already implemented.

The following current behavior must not regress:

- Write uses a continuous Tiptap-backed manuscript editor over `SceneBlockDocument`.
- Write does not return to repeated block cards or one textarea per stored block.
- Codex progression insertion remains behind the top Insert menu.
- Inline Codex progression components remain default-collapsed, expandable, draggable by release target, and UI-resizable.
- Write/Codex progression create/delete continues through scene-level atomic commands.
- Write and Codex autosave remains background work and must not disable active editing fields.
- Codex Canon and Detail editors remain bounded internal-scroll surfaces.
- Codex Details, Relations, Mentions, Progressions, category management, reusable detail types, and entry delete/archive/restore workflows remain intact.
- Settings/AI provider paths must use explicit Provider selection and must not silently fall back to another Provider.
- No UI may expose audit IDs, hashes, prompt versions, or revisions in author-facing main paths unless the spec explicitly calls for a detail/audit view.

## Product Loop

The first accepted M5 loop is:

```text
Seeded or Workshop-created Proposal
  -> Review Inbox
  -> exact Proposal Detail route
  -> accept/reject/edit/stale decision
  -> command adapter applies or refuses the change
  -> source surface reflects the decision
```

Workshop enters the loop later:

```text
Workshop Session
  -> Message
  -> compact context selector backed by WorkshopContextBasket / Context Builder
  -> Model Call
  -> Proposal card
  -> exact Review Proposal Detail
  -> decision
  -> source message status updates
```

Internal tool plans enter after the Proposal/Review loop is stable:

```text
Tool Plan
  -> explicit Grant
  -> validated command adapter
  -> authority write or Proposal fallback
  -> Review/source status
```

## Product Objects

M5 introduces or upgrades these objects.

### Workshop

- `WorkshopSession`: durable conversation container.
- `WorkshopBranch`: optional branch from a message.
- `WorkshopMessage`: author, role, system, tool, or result message.
- `WorkshopContextBasket`: internal selected-context record for the session or current call. The UI must edit it through a compact context selector, not through a permanent right-side basket panel.
- `WorkshopContextItemRef`: pointer to scene, selection, Codex entry, section, research note, prompt/style context, or Review/Proposal source.
- `WorkshopToolDefinition`: internal project tool description, not an external model tool.
- `WorkshopToolPlan`: model- or user-created plan with ordered steps.
- `WorkshopToolStep`: one proposed internal command.
- `WorkshopToolGrant`: explicit user approval scope.
- `WorkshopToolCall`: audited execution attempt.
- `WorkshopToolResult`: completed, skipped, blocked, failed, or converted-to-proposal result.

### Proposal And Review

The existing `packages/contracts/src/proposals.ts` must be upgraded. Do not create a parallel Proposal model.

Required Proposal v2 concepts:

- `type`: `text-insertion`, `text-replacement`, `scene-summary`, `codex-create`, `codex-update`, `detail-type-update`, `relation-update`, `progression-create`, `planning-update`, `research-note`, `continuity-issue`, or equivalent narrowed enum.
- `source`: `workshop-message`, `write-selection`, `codex-entry`, `tool-plan`, `import`, `model-call`, or `manual`.
- `target`: target kind, target ID, author-readable label, base revision, optional field/block/range.
- `contextBundleId`: required for model-backed Proposals, nullable for manual/import/tool-generated Proposals when no bundle exists.
- `generator`: supports `ai`, `manual`, `tool`, and `import`; it must not require a model call for every Proposal.
- `riskLevel`: low, medium, high.
- `confidence`: optional numeric or enum confidence.
- `reason`: short author-facing rationale.
- `staleReason`: populated when target revision, reference, permission, or dependency invalidates the Proposal.
- `supersededBy`: replacement Proposal ID.
- `originalCandidate`: preserved when the user edits and accepts.
- `decision`: accepted/rejected/edited/stale/superseded/archive metadata with actor, timestamp, note, snapshot ID where relevant.
- `evidence`: scene/Codex/research/model-call/source-message references with revisions and quotes where applicable.

### Proposal State Machine

Persistent states:

```text
pending
accepted
rejected
edited
stale
superseded
archived
```

Allowed transitions:

```text
pending -> accepted
pending -> rejected
pending -> edited
pending -> stale
pending -> superseded
pending -> archived
stale -> superseded
stale -> archived
accepted -> archived
rejected -> archived
edited -> archived
superseded -> archived
```

`conflicted` is not a durable Proposal state in M5. It is an application or batch-preview result when a pending/stale Proposal cannot be applied because of current target state, lock, permission, missing reference, or command validation. If later implementation needs durable conflict records, that must be added by an explicit contract update.

## Storage Draft

All M5 authority files must be schema-versioned JSON and written with same-directory temp files, validation, and atomic replacement.

Recommended paths:

```text
workshop/
  sessions/<session-id>.json
  branches/<branch-id>.json
  messages/<message-id>.json

.studio/
  workshop/tool-plans/<plan-id>.json
  workshop/tool-grants/<grant-id>.json
  workshop/tool-calls/<call-id>.json
  inbox/proposals/<proposal-id>.json
  review/batches/<batch-id>.json
  snapshots/<snapshot-id>.json
```

Do not use a JSONL message log as the only durable Workshop message authority unless the implementation first proves atomic append, validation, crash recovery, and partial-line quarantine. The safer default is one JSON file per message plus rebuildable indexes for ordering/search.

SQLite remains rebuildable. It may index sessions, messages, proposals, status, target, source, and search text, but it must never be the only copy of Proposal/Workshop state.

## Server/API Draft

Route names may be refined during implementation, but the task must preserve these capabilities.

### Proposal And Review

- `GET /api/v1/series/:seriesId/proposals`
- `POST /api/v1/series/:seriesId/proposals`
- `GET /api/v1/series/:seriesId/proposals/:proposalId`
- `POST /api/v1/series/:seriesId/proposals/:proposalId/accept`
- `POST /api/v1/series/:seriesId/proposals/:proposalId/reject`
- `POST /api/v1/series/:seriesId/proposals/:proposalId/edit-and-accept`
- `POST /api/v1/series/:seriesId/proposals/:proposalId/mark-stale`
- `POST /api/v1/series/:seriesId/proposals/:proposalId/supersede`
- `POST /api/v1/series/:seriesId/proposals/:proposalId/archive`
- `POST /api/v1/series/:seriesId/proposals/batch-preview`
- `POST /api/v1/series/:seriesId/proposals/batch-accept`

### Workshop

- `GET /api/v1/series/:seriesId/workshop/sessions`
- `POST /api/v1/series/:seriesId/workshop/sessions`
- `GET /api/v1/series/:seriesId/workshop/sessions/:sessionId`
- `PUT /api/v1/series/:seriesId/workshop/sessions/:sessionId`
- `POST /api/v1/series/:seriesId/workshop/sessions/:sessionId/archive`
- `POST /api/v1/series/:seriesId/workshop/sessions/:sessionId/restore`
- `GET /api/v1/series/:seriesId/workshop/sessions/:sessionId/messages`
- `POST /api/v1/series/:seriesId/workshop/sessions/:sessionId/messages`
- `POST /api/v1/series/:seriesId/workshop/sessions/:sessionId/branch`
- `GET /api/v1/series/:seriesId/workshop/sessions/:sessionId/context-basket`
- `PUT /api/v1/series/:seriesId/workshop/sessions/:sessionId/context-basket`
- `POST /api/v1/series/:seriesId/workshop/sessions/:sessionId/context-preview`
- `POST /api/v1/series/:seriesId/workshop/sessions/:sessionId/calls`

### Tool Plans

- `GET /api/v1/series/:seriesId/workshop/tools`
- `POST /api/v1/series/:seriesId/workshop/tool-plans`
- `GET /api/v1/series/:seriesId/workshop/tool-plans/:planId`
- `POST /api/v1/series/:seriesId/workshop/tool-plans/:planId/approve`
- `POST /api/v1/series/:seriesId/workshop/tool-plans/:planId/reject`
- `POST /api/v1/series/:seriesId/workshop/tool-plans/:planId/convert-to-proposal`
- `GET /api/v1/series/:seriesId/workshop/tool-calls/:toolCallId`

## Command Adapter Boundary

Accepted Proposals and authorized Tool Plans must share the same command adapter layer.

Rules:

- AI may request a Proposal or Tool Plan, but it cannot execute authority writes.
- The server validates grant, target, base revision, references, permission, and risk.
- Tool adapters call existing domain/storage commands. They must not write authority files directly.
- Dangerous actions require explicit confirmation every time.
- A grant is scoped by series, session, tool plan, tool definition, target, risk, and expiration.
- New or changed plan steps require renewed approval.
- Partial failure must report completed, skipped, failed, and blocked steps.

Tool coverage is allowed only after Proposal/Review application is stable.

## Contract-Informed Figma

Figma remains mandatory before implementation, but Figma acceptance must be based on product objects and state transitions, not a static button inventory. Agents must not perform screenshot-based UI acceptance for this project. Implementation must use a Figma checklist covering node ID, route, entry/return paths, layout structure, row/control sizing, component mapping, data backing, and deferred controls. User visual acceptance remains the only visual acceptance gate.

Required design basis:

- Proposal v2 object and state machine.
- Review decision loop.
- Workshop Session/Message/Context Basket objects.
- Tool Plan/Grant/Tool Call objects.
- Route and entry-point map.

Required user tasks in Figma:

1. Author opens Workshop and continues a session.
2. Author selects context and previews what the role can read.
3. Author receives a message that creates a Proposal card.
4. Author opens the exact Proposal detail from the card.
5. Author accepts, rejects, edits, or marks stale in Review.
6. Review links back to the source Workshop message.
7. A stale/conflict/failure/permission-denied state gives a recoverable next step.
8. Codex and Write tool actions are visually gated by Proposal or explicit Grant.

Buttons must exist only when their route, state, and backing behavior are defined or explicitly marked deferred.

## Controlled Vertical Slice Plan

### M5.0 Current Feature Protection Lock

Status:

- Passed on 2026-07-01.
- Added targeted protection tests for honest Review/Workshop unavailable shells and JSON-only Codex Progression authority.
- Focused checks, full build, full test, and `git diff --check` passed.

Goal:

- Freeze the current Write/Codex/Settings/AI non-regression contract before M5 changes.

Allowed changes:

- Tests and docs only unless a test exposes a current breakage.
- Add regression mapping for existing Write/Codex behavior that M5 must not remove.

Not allowed:

- No Workshop/Review implementation yet.
- No Proposal schema replacement yet.

Exit checks:

- Current focused Write/Codex web tests pass.
- Current storage/server tests for scene document, progression, context, and Codex routes pass.
- M5 acceptance record maps protected behavior to tests.

Acceptance coverage:

- `M5-A01`.

### M5.1 Proposal v2 Contract And State Machine

Status:

- Command/function verified on 2026-07-01.
- Upgraded the existing Proposal contract and state-machine tests without creating a parallel Proposal model.

Goal:

- Upgrade existing Proposal contracts without creating a parallel model.

Allowed changes:

- `packages/contracts/src/proposals.ts`.
- Contract tests.
- Task/API/Data Model documentation updates.

Not allowed:

- No storage writes.
- No Review UI.
- No Workshop UI.

Exit checks:

- Contract build passes.
- Tests cover valid/invalid Proposal types, sources, generator kinds, decisions, stale reason, supersession, and state transitions.

Acceptance coverage:

- `M5-A02`, `M5-A03`, `M5-A04`.

### M5.2 Proposal/Review Storage And API

Status:

- Command/function verified on 2026-07-01.
- Added Proposal JSON storage, Review/proposal routes, stale/decision paths, snapshot behavior, and batch preview/accept reporting.

Goal:

- Create, read, stale-check, reject, accept, edit-and-accept, supersede, archive, and batch-preview seeded Proposals.

Allowed changes:

- JSON Proposal storage.
- Snapshot creation before accepted writes.
- Review/proposal server routes.
- Command adapter skeleton.
- Tests for seeded Proposals.

Not allowed:

- No Workshop generation yet.
- No broad UI except minimal API clients if needed.
- No fake accept path that bypasses existing domain/storage commands.

Exit checks:

- Storage/server tests cover round trip, corrupted Proposal isolation, duplicate ID rejection, missing source handling, stale base revision, reject, accept, edit-and-accept, supersede, archive, and batch preview.
- Accepted Proposal uses command adapter and creates snapshot.
- Batch preview excludes stale/conflicted items.

Acceptance coverage:

- `M5-A05` through `M5-A12`.

### M5.3 Review UI First

Status:

- Command/function verified on 2026-07-01.
- Post-audit repair on 2026-07-01 closed the retained M5.1-M5.4 findings for stale marking, scene-content base revisions, batch revision binding, intra-batch conflicts, multi-file Proposal acceptance transactions, source availability validation, full patch review display, batch result display, Workshop context additions, excluded context preview rendering, and feature-local Review/Workshop CSS.
- Review now uses real Proposal APIs for inbox/detail/decision flows. User visual acceptance is pending.

Goal:

- Replace the unavailable Review shell with a real Proposal inbox and exact Proposal detail driven by Proposal APIs.

Allowed changes:

- `apps/web/src/features/review/ReviewWorkspace.tsx`.
- Routing/state needed to open `/review/proposals/:proposalId` or equivalent app state.
- API client additions.
- Centralized UI text.
- Visual implementation of accepted Figma Review pages.

Not allowed:

- No fake Proposal counts.
- No Workshop chat implementation.
- No unbacked Review actions.

Exit checks:

- Web tests cover Review inbox, exact Proposal detail deep link, accept, reject, stale display, unavailable source, batch preview, and no main-path audit field exposure.
- Figma checklist review is required before claiming implementation alignment. Agent-owned screenshot acceptance is prohibited; user visual acceptance remains separate.
- User visual acceptance remains separate.

Acceptance coverage:

- `M5-A13` through `M5-A17`.

### M5.4 Workshop Sessions, Context Basket, And Single-Role Call

Status:

- Command/function verified on 2026-07-01.
- Post-audit repair on 2026-07-01 closed the retained M5.1-M5.4 Workshop findings for allowed current UI context additions, excluded context preview details, and feature-local Workshop CSS.
- Workshop now persists sessions/messages, stores context baskets, previews context through the existing Context Builder, selects from library-global model settings plus provider model overrides, and runs single-role calls without authority mutation. User visual acceptance is pending.
- 2026-07-02 follow-up: the visible Workshop UI no longer exposes a right-side Context Basket or separate Preview Context button. Context selection is edited through a nested composer menu that supports full novel text, full outline, acts, chapters, multiple scenes, direct Codex entries, entries by type, entries by detail type, and entries by category. Selecting a story scope materializes linked Codex entries into the same internal context basket when Codex policy allows it. User visual acceptance remains pending.
- 2026-07-02 follow-up: Workshop General Chat is now the default mode. It sends a user-editable system prompt into Context Builder, shows the author's submitted message in the chat immediately, appends the assistant reply or model-error message when the call resolves, and does not expose Create Proposal for General Chat replies. The system prompt editor must not be a cramped permanent footer textarea; it is accessed through a compact footer button and opens as a larger editor popover. Storage/API also reject Proposal creation from General Chat messages.

Goal:

- Make Workshop a persistent conversation and context-selection workspace.

Allowed changes:

- Workshop session/message storage and API.
- Context Basket storage/API.
- Context preview integration with existing M4 Context Builder.
- Single-role call that creates ContextBundle and ModelCallLog.
- Global model setting and per-call provider model selection for Workshop calls.
- Workshop UI implementation from accepted Figma/current user-directed compact-selector plan.

Not allowed:

- No tool execution.
- No council yet.
- No authority write from model output.

Exit checks:

- Storage/server/web tests cover session/message round trip, branch basics, context basket update, context preview exclusions, model failure preserving input/context, and no empty Proposal creation on model failure.
- Web tests cover selecting a saved model setting, fetching provider models, selecting a provider model, and sending that model override with the Workshop call.
- Workshop reloads durable messages.
- Main UI does not show audit fields.
- Figma checklist review is required before claiming implementation alignment. Agent-owned screenshot acceptance is prohibited; user visual acceptance remains separate.

Acceptance coverage:

- `M5-A18` through `M5-A24`.

Implemented scope addendum:

- The composer context selector replaces the rejected visible Context Basket panel and the rejected separate Preview Context control.
- Full-novel, full-outline, act, chapter, and selected-scene context scopes are first-class context item kinds in contracts, server context assembly, Workshop route mapping, storage validation, and web UI.
- Selecting an act, chapter, scene, full outline, or full novel automatically links Codex entries whose entry policy is `always` or whose name/alias is detected under `on-mention`; `manual` and `never` entries are not auto-linked.
- Frontend and storage both materialize linked Codex entries with `note: "Linked from selected context."` so the user-visible menu and the persisted request context stay aligned.
- The General Chat system prompt is a hidden-until-open editor popover from the composer footer, not a permanently visible bottom-bar text field.

### M5.5 Workshop Message To Proposal Deep Link

Goal:

- Workshop messages can create Proposal records that link to exact Review detail pages and back to the source message.

Status:

- Command/function verified on 2026-07-01.
- Post-audit repair on 2026-07-01 closed M5.5 findings for server batch-accept route tests, transactional Workshop message-to-Proposal writes, broader Proposal card status coverage, unavailable source return UI, context-scene targeting, duplicated CSS, and stale docs status.
- User visual acceptance remains pending, and agent-owned screenshot acceptance is prohibited.

Allowed changes:

- Proposal source linkage to Workshop messages.
- Workshop Proposal cards.
- Review source-message return path.
- Status sync between Review decision and Workshop card.

Not allowed:

- No Tool Plan execution yet.
- No council yet.

Exit checks:

- Tests cover message-to-Proposal creation, exact deep link, missing source message recoverable state, Review decision syncing back to Workshop, and archived/unavailable Proposal link behavior.
- Tests also cover rejected, edited, stale, superseded, archived, and missing Proposal card states in Workshop.
- Figma checklist review is required before claiming implementation alignment. Agent-owned screenshot acceptance is prohibited; user visual acceptance remains separate.

Acceptance coverage:

- `M5-A25` through `M5-A29`.

Implemented scope:

- Workshop messages now create durable Proposal records through a Workshop-scoped API that derives `source` and `generator` from the source message instead of trusting the client.
- Workshop message-to-Proposal creation writes the Proposal authority file and the source message `proposalIds` update through one transaction.
- Workshop messages persist `proposalIds`, while Proposal status remains authoritative in Proposal JSON files.
- Workshop Proposal cards deep-link to `#/review/proposals/:id`.
- Review Proposal Detail can return to the exact source Workshop message via `#/workshop/sessions/:sessionId/messages/:messageId`.
- Missing source messages and archived source sessions become recoverable unavailable states instead of dead navigation.
- Workshop-created scene-content Proposals target the Workshop basket scene when available rather than blindly using the currently selected app scene.

### M5.6 Tool Plan, Grant, And Command Adapter

Goal:

- Let Workshop propose internal project tool actions that can run only with explicit user permission or convert to Proposal.

Allowed changes:

- Tool definitions for approved Codex and Write operations.
- Tool Plan/Grant/Tool Call storage/API.
- Command adapter implementation for safe subset.
- UI for permission and tool result states.

Not allowed:

- No shell/script/external file tools.
- No direct authority write from AI.
- No dangerous action without explicit approval.

Exit checks:

- Tests cover expired grants, unauthorized tool rejection, changed plan requiring renewed approval, dangerous action confirmation, stale target refusal, partial failure reporting, and Proposal fallback.
- Write/Codex authority commands still pass existing tests.

Acceptance coverage:

- `M5-A30` through `M5-A37`.

### M5.7 Council, Batch, Conflict/Failure, Responsive, And Final Visual Acceptance

Goal:

- Complete M5's broader editor-team behavior and final acceptance path.

Allowed changes:

- Council first-round independent calls.
- Summary step that only cites first-round outputs.
- Batch accept implementation.
- Conflict/failure/narrow layout states.
- Final visual QA and user acceptance records.

Not allowed:

- No automatic background authority mutation.
- No hidden future information in council/context.
- No completion claim before user visual acceptance.

Exit checks:

- Tests prove council isolation, summary citation boundary, batch accept completed/skipped/failed reporting, conflict behavior, future Progression isolation, and Figma checklist coverage.
- User visual acceptance is explicitly recorded before M5 is marked product/visual accepted.

Acceptance coverage:

- `M5-A38` through `M5-A45`.

## Acceptance ID Map

| ID | Requirement |
| --- | --- |
| M5-A01 | M5 changes do not regress current Write, Codex, Settings, AI provider, context, or JSON authority behavior. |
| M5-A02 | Proposal v2 extends the existing Proposal contract and does not introduce a parallel Proposal model. |
| M5-A03 | Proposal type/source/generator/target/decision schemas support AI, manual, tool, and import origins. |
| M5-A04 | Proposal state transitions enforce pending, accepted, rejected, edited, stale, superseded, and archived semantics. |
| M5-A05 | Proposal JSON storage round trips valid records and isolates corrupted records from the rest of the inbox. |
| M5-A06 | Duplicate Proposal IDs are rejected or quarantined with diagnostics. |
| M5-A07 | Missing source message/model/context references produce recoverable unavailable states. |
| M5-A08 | Stale base revisions cannot be accepted. |
| M5-A09 | Accept creates a snapshot before mutating authority data. |
| M5-A10 | Edit-and-accept preserves the original candidate and stores the edited result. |
| M5-A11 | Batch preview excludes stale/conflicted items and explains why. |
| M5-A12 | Batch accept reports completed, skipped, failed, and blocked items if any step fails. |
| M5-A13 | Review Inbox lists real Proposal data with filtering and no fake counts. |
| M5-A14 | Review Proposal Detail opens by exact Proposal ID. |
| M5-A15 | Review accept/reject/edit/stale actions call real APIs and update state. |
| M5-A16 | Review main path hides engineering audit fields while keeping details reachable. |
| M5-A17 | Review visual implementation is checked against the Figma structure checklist, with user visual acceptance tracked separately. |
| M5-A18 | Workshop sessions and messages persist as schema-versioned JSON and reload after restart. |
| M5-A19 | Workshop selected context can add/remove/toggle allowed references, including full novel text, full outline, act, chapter, multiple scenes, direct Codex entries, and Codex grouped by type/detail/category. |
| M5-A20 | Context assembly shows included/excluded items and respects permissions, future-story isolation, auto-linked Codex policy, and per-detail Send to AI switches. |
| M5-A21 | Single-role Workshop call creates ContextBundle and ModelCallLog. |
| M5-A22 | Model failure preserves the input and context and does not create an empty Proposal. |
| M5-A23 | Workshop message UI remains author-facing and does not expose main-path audit fields. |
| M5-A24 | Workshop visual implementation is checked against the Figma structure checklist, with user visual acceptance tracked separately. |
| M5-A25 | Workshop message output can create a Proposal linked to that exact source message. |
| M5-A26 | Proposal cards deep-link to exact Review Proposal Detail. |
| M5-A27 | Review Detail links back to the source Workshop message. |
| M5-A28 | Workshop Proposal cards reflect accepted, rejected, edited, stale, superseded, and unavailable states. |
| M5-A29 | Archived/unavailable Proposal links show recoverable unavailable states instead of dead navigation. |
| M5-A30 | Tool Plans are durable JSON records and cannot execute without a valid Grant. |
| M5-A31 | Expired or mismatched Grants cannot execute tools. |
| M5-A32 | Unauthorized tools cannot write authority data. |
| M5-A33 | Tool adapters call existing domain/storage commands instead of direct file writes. |
| M5-A34 | Dangerous actions require explicit confirmation and cannot be covered by broad low-risk grants. |
| M5-A35 | Tool execution validates target revisions and refuses stale writes. |
| M5-A36 | Tool partial failure reports completed, skipped, failed, and blocked steps. |
| M5-A37 | Tool Plan conversion to Proposal preserves source, reason, evidence, and target revisions. |
| M5-A38 | Council first-round role calls are independent and do not receive other roles' outputs. |
| M5-A39 | Council summary cites first-round outputs and does not introduce unchecked new conclusions. |
| M5-A40 | Council and Workshop context do not leak future Codex Progression, future scene text, or hidden sections. |
| M5-A41 | Batch accept applies only eligible Proposals and preserves skipped/failed diagnostics. |
| M5-A42 | Conflict and stale states are recoverable and do not silently merge. |
| M5-A43 | Narrow/responsive states follow accepted Figma behavior and do not fake a mobile editor if desktop space is required. |
| M5-A44 | User-requested visual artifacts, if any, are page-scoped and do not become agent-owned screenshot acceptance. |
| M5-A45 | M5 is not marked complete until command tests pass and user visual acceptance is explicitly recorded. |

## Required Commands Before Completion

The exact command set may expand by slice. At minimum:

```powershell
npm.cmd run build -w @novel-studio/contracts
npm.cmd run test -w @novel-studio/storage -- repository.test.ts
npm.cmd run test -w @novel-studio/server -- app.test.ts context-routes.test.ts
npm.cmd run test -w @novel-studio/web -- AppShell.test.tsx EditorSurface.test.tsx
npm.cmd run build
npm.cmd run test
git diff --check
```

Visual implementation slices must not run agent-owned screenshot acceptance. They must use Figma structure checklists, component/data mapping, focused functional tests, and explicit user visual review.

## Completion Criteria

M5 is complete only when:

- Figma design for the implemented M5 scope is accepted by the user.
- Proposal v2 contracts and state machine are implemented and tested.
- Review can decide real Proposals before Workshop-generated Proposals are claimed.
- Workshop has persistent sessions, messages, compact context selection backed by persisted context data, Context Builder assembly, and single-role calls.
- Workshop messages can create Proposal cards that deep-link to exact Review detail pages.
- Review decisions sync back to the source Workshop message.
- Authorized Tool Plans can execute approved Codex and Write commands through shared adapters, or safely convert to Proposals.
- Council first-round independence is tested.
- Batch accept, stale, conflict, failure, permission, and narrow states are tested.
- Existing Write/Codex/Settings/AI behavior remains covered and passing.
- `docs/testing/M5_ACCEPTANCE.md` records actual command results.
- `STATUS.md`, `TASKS.md`, `HANDOFF.md`, and related records are updated only with actual state, not projected plans.
