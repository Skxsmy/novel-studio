# NS-408：真实 Provider 接入

状态：进行中。当前完成 DeepSeek 独立 Provider 与通用 OpenAI-compatible 基础路径；其余 Provider 仍待实现。

## 目标

在统一 `ProviderAdapter` 下接入真实模型服务，让后续上下文装配、提示词渲染、流式调用和调用日志不再只能依赖 `MockProvider`。

首条真实路径采用 DeepSeek 官方 OpenAI 格式接口，但在数据模型和 ProviderRegistry 中作为独立 `deepseek` Provider 管理；通用 `openai-compatible` 只表示用户自行填写服务地址的兼容服务，不继承 DeepSeek 默认地址、模型或错误语义。

## 本阶段已实现

- 新增 `OpenAiCompatibleProvider` 传输层，并用它分别承载 `deepseek` 与通用 `openai-compatible`。
- 默认注册 `mock`、`deepseek` 与 `openai-compatible` 三个 Provider。
- 支持 DeepSeek 与通用 OpenAI-compatible 服务：
  - 连接测试；
  - 模型列表；
  - 流式文本；
  - 基础结构化输出；
  - 本地 token 粗估；
  - 统一错误分类。
- DeepSeek 快速配置入口：
  - 默认服务地址：`https://api.deepseek.com`；
  - 默认模型代号：`deepseek-v4-flash`；
  - 上下文窗口声明：`1,000,000 tokens`。
- DeepSeek 与通用兼容服务已拆分：
  - DeepSeek 使用 `provider: deepseek`；
  - 自定义兼容服务使用 `provider: openai-compatible`；
  - 通用兼容服务没有 DeepSeek 默认模型，必须由用户填写服务地址或从服务端模型列表选择。
- API Key 保存入口：
  - 用户在设置页输入；
  - 服务端写入 `CredentialStore`；
  - 模型配置只保存凭据引用；
  - 输入框保存后清空；
  - 用户可替换、删除当前密钥，也可让模型配置复用已有凭据引用；
  - 明文密钥不写入作品目录、日志或 Git。
- 用户侧真实验收：
  - 用户已在设置页保存 DeepSeek 密钥；
  - DeepSeek 连接测试正常；
  - 能够从 DeepSeek 获取模型列表。
- 后端路由统一使用注入的 `ProviderRegistry` 和 `CredentialStore`，避免测试或后续 Provider 接入时依赖模块级单例。
- 浏览器验收截图改为每次生成唯一运行 ID、唯一文件名和 `*-screenshot-manifest.json`，避免同名截图被误认为最新证据。

## 本阶段未完成

- 未实现独立 OpenAI、OpenRouter、Anthropic、Gemini、Ollama adapter。
- 尚未记录用户侧真实 DeepSeek 非写入调用结果。
- 未提供完整 Provider 回退链 UI。
- 未提供完整调用记录浏览界面；仍属 `NS-409`。
- 未实现 provider-specific tokenizer；当前仍是统一粗估。

## 用户界面边界

设置页只应展示用户当前需要做的事：

- 选择或新增模型配置；
- 填写服务地址和模型代号；
- 保存服务密钥；
- 替换、删除或复用已保存密钥；
- 获取服务商返回的模型列表；
- 测试连接。

开发者边界、失败回退、日志脱敏、调用哈希、提示词版本、上下文快照等审计信息不得塞进主界面。需要展示时应进入“高级信息”、调用记录、帮助页或任务文档。

## 验收标准

| 编号 | 标准 | 状态 |
|---|---|---|
| NS408-A01 | 默认 registry 注册 `mock`、`deepseek` 和 `openai-compatible` | 已通过 |
| NS408-A02 | DeepSeek 配置可从设置页创建，显示服务地址、模型代号、密钥入口和连接状态 | 已通过 |
| NS408-A03 | API Key 只经凭据端点保存，不写入模型配置明文 | 已通过 |
| NS408-A04 | DeepSeek adapter 可用 fake fetch 验证官方 `/models` 与 `/chat/completions` SSE 兼容响应 | 已通过 |
| NS408-A05 | 服务端连接测试和模型调用可注入 fake credential store / fake fetch 验证，不需要真实密钥 | 已通过 |
| NS408-A06 | 认证失败等错误不泄露密钥 | 已通过 |
| NS408-A07 | 浏览器截图有唯一运行 ID 和 manifest，人工检查截图而非复用旧图 | 已通过 |
| NS408-A08 | DeepSeek 与通用 OpenAI-compatible 不共享默认地址、模型或 provider-specific 错误语义 | 已通过 |
| NS408-A09 | 用户保存真实 DeepSeek 密钥后，完成真实连接测试并获取模型列表 | 用户侧通过 |
| NS408-A10 | 用户保存真实 DeepSeek 密钥后，完成一次真实非写入调用 | 待执行 |
| NS408-A11 | OpenAI、OpenRouter、Anthropic、Gemini、Ollama adapter | 待实现 |

## 已运行测试

- `npm.cmd run typecheck`
- `npm.cmd run test -w @novel-studio/ai`：15/15
- `npm.cmd run test -w @novel-studio/server`：15/15
- `npm.cmd run check`：通过；Server 15/15，Web 14/14，AI 15/15，Storage 41/41，生产构建通过
- `npm.cmd run test:e2e`：通过；1 个 Chrome 用例

## 下一步

继续补真实 DeepSeek 非写入调用记录，或开始下一个 Provider。若继续 Provider，优先补 Ollama 或 OpenAI；若先整理 M4 主路径，进入 `NS-409` 的上下文预览 UI 与调用日志 UI。
